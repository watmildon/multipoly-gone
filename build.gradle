plugins {
    id 'java'
    id 'org.openstreetmap.josm' version '0.8.2'
}

josm {
    pluginName = 'multipoly-gone'
    josmCompileVersion = 19439
    manifest {
        description = 'Dissolve unnecessary multipolygon relations into simpler closed ways'
        mainClass = 'org.openstreetmap.josm.plugins.multipolygone.MultipolyGonePlugin'
        minJosmVersion = 19439
    }
}

tasks.withType(JavaExec).configureEach {
    jvmArgs += [
        '--add-exports=java.base/sun.security.action=ALL-UNNAMED',
        '--add-exports=java.desktop/com.sun.imageio.plugins.jpeg=ALL-UNNAMED',
        '--add-exports=java.desktop/com.sun.imageio.spi=ALL-UNNAMED'
    ]
}

repositories {
    mavenCentral()
}

dependencies {
    packIntoJar 'org.locationtech.jts:jts-core:1.20.0'
    // Also add as implementation so IDEs see JTS on the classpath
    implementation 'org.locationtech.jts:jts-core:1.20.0'
    testImplementation 'org.junit.jupiter:junit-jupiter:5.10.2'
}

test {
    useJUnitPlatform()
    jvmArgs += [
        '--add-exports=java.base/sun.security.action=ALL-UNNAMED',
        '--add-exports=java.desktop/com.sun.imageio.plugins.jpeg=ALL-UNNAMED',
        '--add-exports=java.desktop/com.sun.imageio.spi=ALL-UNNAMED'
    ]
    maxHeapSize = '1g'
    testLogging {
        showStandardStreams = true
    }
}

sourceSets {
    test {
        resources {
            srcDir 'tests'
        }
    }
}
